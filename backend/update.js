const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); async function run() { await pool.query('UPDATE withdrawal_settings SET min_withdrawal_tasky = 1000'); await pool.query("UPDATE swap_rates SET min_tasky = 1000, tasky_per_unit = 0.5 WHERE token_name = 'DOGS'"); console.log('updated db'); process.exit(0); } run();
